import React from 'react'
import './AppDownload.css'

const AppDownload = () => {
    return (
        <div className='app-download' id='app-download'>
            <p>Crave runs entirely on the web right now. <strong>No app-store detour, no install friction.</strong></p>
            <span className="app-download-note">Search, customise, pay and reorder directly from any phone browser.</span>
        </div>
    )
}

export default AppDownload
